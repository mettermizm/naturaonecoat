import { defineComponent, inject } from "vue";
var Variant = defineComponent({
  name: "HistoireVariant",
  props: {
    title: {
      type: String,
      default: "untitled"
    },
    id: {
      type: String,
      default: null
    },
    icon: {
      type: String,
      default: null
    },
    iconColor: {
      type: String,
      default: null
    }
  },
  setup(props) {
    var _a;
    const story = inject("story");
    function generateId() {
      return `${story.id}-${story.variants.length}`;
    }
    const variant = {
      id: (_a = props.id) != null ? _a : generateId(),
      title: props.title,
      icon: props.icon,
      iconColor: props.iconColor
    };
    const addVariant = inject("addVariant");
    addVariant(variant);
  },
  render() {
    return null;
  }
});
export { Variant as default };
