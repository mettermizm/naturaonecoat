import { defineStore } from "@histoire/vendors/pinia";
import { useStorage } from "@histoire/vendors/vue-use";
const usePreviewSettingsStore = defineStore("preview-settings", () => {
  const currentSettings = useStorage("_histoire-sandbox-settings-v2", {
    responsiveWidth: 720,
    responsiveHeight: null,
    rotate: false,
    backgroundColor: "transparent",
    checkerboard: false
  });
  return {
    currentSettings
  };
});
export { usePreviewSettingsStore };
