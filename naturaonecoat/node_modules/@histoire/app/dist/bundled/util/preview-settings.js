import { reactive } from "@histoire/vendors/vue";
const receivedSettings = reactive({});
function applyPreviewSettings(settings) {
  Object.assign(receivedSettings, settings);
}
export { applyPreviewSettings, receivedSettings };
